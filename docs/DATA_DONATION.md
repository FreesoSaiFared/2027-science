# Data Donation

Donation is explicit, redacted, scoped, and expiring. A donated quest profile contains interests, languages, preferences needed for the task, and expiry metadata. Users can revoke. The full Mask is never uploaded by default.
