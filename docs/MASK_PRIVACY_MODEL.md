# Mask Privacy Model

The Mask is a local pseudonymous profile. Default is Anonymous Mode. Timeout, inactivity, scrolling, or page presence never implies consent. Export/import is local. Observatory donation sends only a redacted quest profile and expires.
