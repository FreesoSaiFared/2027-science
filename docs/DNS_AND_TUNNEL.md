# DNS and Tunnel

Registrar is Porkbun; authoritative DNS is Cloudflare. Cloudflare Tunnel is for local lab preview and origin experiments. This repo includes `ops/cloudflared/config.example.yml` but does not overwrite production tunnel config.
