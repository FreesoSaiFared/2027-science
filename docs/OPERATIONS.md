# Operations

Use `npm run ops:check` for local non-mutating checks. Use `npm run cf:dev` for Worker preview and `npm run cf:deploy` only after build/check pass. Do not mutate DNS/tunnel/registrar settings without explicit confirmation.
